{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Verdant Habits application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user.",
          "format": "uuid"
        },
        "email": {
          "type": "string",
          "description": "Email address of the user.",
          "format": "email"
        },
        "displayName": {
          "type": "string",
          "description": "The display name of the user."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the user account was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "createdAt",
        "displayName"
      ]
    },
    "Habit": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Habit",
      "type": "object",
      "description": "Represents a habit being tracked by a user.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the habit.",
          "format": "uuid"
        },
        "userId": {
          "type": "string",
          "description": "Reference to User. (Relationship: User 1:N Habit)"
        },
        "name": {
          "type": "string",
          "description": "Name of the habit (e.g., 'Drink Water', 'Exercise')."
        },
        "description": {
          "type": "string",
          "description": "A more detailed description of the habit."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the habit was created.",
          "format": "date-time"
        },
        "completions": {
          "type": "object",
          "description": "A map of completion dates to a boolean status.",
          "additionalProperties": {
            "type": "boolean"
          }
        }
      },
      "required": [
        "id",
        "userId",
        "name",
        "createdAt",
        "completions"
      ]
    },
    "PomodoroSession": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Pomodoro Session",
      "type": "object",
      "description": "Represents a completed Pomodoro focus session.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "Reference to the User."
        },
        "completedAt": {
          "type": "string",
          "description": "Timestamp indicating when the session was completed.",
          "format": "date-time"
        },
        "duration": {
          "type": "number",
          "description": "Duration of the focus session in minutes."
        }
      },
      "required": [
        "userId",
        "completedAt",
        "duration"
      ]
    },
    "WeeklyTask": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Weekly Task",
      "type": "object",
      "description": "Represents a task for a specific week.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the weekly task.",
          "format": "uuid"
        },
        "userId": {
          "type": "string",
          "description": "Reference to User."
        },
        "name": {
          "type": "string",
          "description": "Name of the task (e.g., 'Complete project report')."
        },
        "weekStartDate": {
          "type": "string",
          "description": "The start date of the week for this task (e.g., '2024-01-22').",
          "format": "date"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp indicating when the task was created.",
          "format": "date-time"
        },
        "completions": {
          "type": "object",
          "description": "A map of completion dates to a boolean status for the 7 days of the week.",
          "additionalProperties": {
            "type": "boolean"
          }
        }
      },
      "required": [
        "id",
        "userId",
        "name",
        "weekStartDate",
        "createdAt",
        "completions"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profile information. This collection is path-based and owned by the user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/habits/{habitId}",
        "definition": {
          "entityName": "Habit",
          "schema": {
            "$ref": "#/backend/entities/Habit"
          },
          "description": "Stores habits for a specific user. This subcollection is path-based and owned by the user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            },
            {
              "name": "habitId",
              "description": "The unique identifier of the habit."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/pomodoroSessions/{sessionId}",
        "definition": {
          "entityName": "PomodoroSession",
          "schema": {
            "$ref": "#/backend/entities/PomodoroSession"
          },
          "description": "Stores completed Pomodoro sessions for a user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            },
            {
              "name": "sessionId",
              "description": "The unique identifier of the session."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/weeklyTasks/{taskId}",
        "definition": {
          "entityName": "WeeklyTask",
          "schema": {
            "$ref": "#/backend/entities/WeeklyTask"
          },
          "description": "Stores weekly tasks for a specific user.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user."
            },
            {
              "name": "taskId",
              "description": "The unique identifier of the task."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore structure is designed to optimize for the application's focus on user-specific habit tracking and progress visualization. By structuring data under user-specific collections, we ensure data isolation and efficient querying. The structure prioritizes path-based ownership and avoids complex authorization logic, making security rules simpler and more robust.\n\n*   `/users/{userId}`: Stores user profiles.\n*   `/users/{userId}/habits/{habitId}`: Stores habits owned by a specific user, with completions stored as a map on the habit document for efficient retrieval.\n\n**Authorization Independence (CRITICAL):**\n\nThis design inherently supports Authorization Independence. Because habits are nested under the user's document, access control can be easily enforced based on the authenticated user ID (`request.auth.uid`). No `get()` calls are needed to verify ownership, as the path itself establishes the relationship.\n\n**QAPs (Rules are not Filters):**\n\nThe chosen structure facilitates secure `list` operations because all documents within a collection share the same security requirements. For example, listing habits under `/users/{userId}/habits` only requires verifying that `request.auth.uid == userId`. This structure avoids the need to filter based on document content, ensuring QAPs are met."
  }
}
